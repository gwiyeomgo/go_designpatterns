기존코드를 건드리지 않고 새로운 기능을 추가하는 방법을 제안하는 패턴

더블 디스패치?

다형성에서 분배 배치 를 위임하는 과정이 디스패치

element 는 변하지 않는 클래스

구체적인 타입을 찾아가는 과정 2번 발생 = 더블 디스패치

 if else 문 없이 기존 코드가 정리됨
 

장점
기존 코드를 수정하지 않고 코드를 추가할 수 있다


단점
구조가 너무 복잡
엘리먼트가 추가되거나 없어지면 상당히 많은 변경이 필요하다
