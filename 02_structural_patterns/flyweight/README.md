Flyweight

어떤 클래스의 인스턴스 한 개만 가지고 여러 가상 인스턴스를 제공하고 싶을 때 사용하는 패턴
객체를 1번만 생성하고
객체를 공유하는 모습


객체 인스턴스의 개수를 줄여서 메모리를 절약
메모리를 최소한으로 사용해야 하는 경우에 사용
클래스의 객체를 많이 만들어야 할 때 사용


* 싱글통 패턴과의 차이
  * 싱글톤 패턴은 클래스 자체가 오직 1개 인스턴스만 허용
  * 플라이웨이트 패턴은 클래스 팩토리에서 제어
